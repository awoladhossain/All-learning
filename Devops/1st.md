
3 tier architecture -> frontend, backend, database
MLops
ML, Algorithm

touch, cat, vim, i, wq
rm,
file system
git-version control system(vcs) track changes + collaboration

sudo(super user do)
git repository

commit id/hash
3 stage of git
1.your working directory(its unstaged and the color is red) -> git status
2.staging area(its staged and the color is green) -> git add
3.your repository(its committed and the color is blue) -> git commit and we created a commit id/hash and version(local repository version of the code that is in my local machine)

git config --global user.name "your name"(for double word you have to give "your name")
git config --global user.email "your email"

for untrack file or directory
rm -rf .git (it means remove recursively)
